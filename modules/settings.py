# settings.py inside modules folder

def get_module_name():
    """Return the name of the module to display in the dropdown."""
    return "Settings"

def execute_module_function():
    """A sample function to execute when the 'Settings' module is selected."""
    # This is just a simple example; you can expand this function to
    # open a settings dialog, modify configurations, etc.
    print("Settings module selected!")